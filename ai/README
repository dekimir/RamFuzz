Utilities for running artificial-intelligence operations on top of RamFuzz, eg,
generation of training corpuses, neural network architectures, mechanisms to
feed the learned knowledge back to RamFuzz runtime, etc.  Each source file here
should have self-describing comments.
