RamFuzz runtime library: classes and functions used to procure random parameter
values from valgen.  Referenced extensively by ramfuzz-generated test code, but
also usable directly.  The runtime library is header-only, residing entirely
within the ramfuzz-rt.hpp file.

Read ramfuzz-rt.hpp first.
